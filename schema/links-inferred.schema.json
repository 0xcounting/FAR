{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/0xcounting/far/schema/links-inferred.schema.json",
  "title": "FAR model-inferred DTI links",
  "description": "Links asserted by a language model from names and tickers alone, because the free DTI snapshot redacts the contract address. Published at status 'inferred' — the lowest tier. Generated by scripts/ingest-agent-results.js; not hand-edited. Promoting a link means moving it into data/links.json by reviewed pull request.",
  "type": "object",
  "required": ["assertedBy", "links"],
  "properties": {
    "assertedBy": { "type": "string" },
    "verdictCounts": { "type": "object" },
    "links": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["dti", "coingeckoId", "reasoning", "assertedBy"],
        "properties": {
          "dti": { "type": "string", "pattern": "^[0-9A-Z]{9}$" },
          "coingeckoId": { "type": "string" },
          "modelConfidence": { "enum": ["high", "medium", "low", null] },
          "reasoning": {
            "description": "What about the names, tickers or types made the model decide. This is what a disputer argues with.",
            "type": "string"
          },
          "assertedBy": { "type": "string" }
        }
      }
    }
  }
}
